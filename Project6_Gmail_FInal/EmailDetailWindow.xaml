<Window x:Class="Project6_Gmail_FInal.EmailDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Перегляд листа" Height="500" Width="700"
        WindowStartupLocation="CenterScreen" Background="#F0F2F5">
    <Grid>
        <Border Background="White" Margin="20" CornerRadius="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.1" ShadowDepth="2"/>
            </Border.Effect>

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="SubjectTxt" Grid.Row="0" FontSize="22" FontWeight="Bold" 
                           TextWrapping="Wrap" Foreground="#202124" Margin="0,0,0,10"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Від: " FontWeight="SemiBold" Foreground="Gray"/>
                    <TextBlock x:Name="FromTxt" Foreground="#0078D4"/>
                </StackPanel>

                <TextBlock x:Name="DateTxt" Grid.Row="2" FontSize="11" Foreground="Gray" Margin="0,0,0,15"/>

                <Separator Grid.Row="2" VerticalAlignment="Bottom"/>

                <ScrollViewer Grid.Row="3" Margin="0,15,0,0" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="BodyTxt" TextWrapping="Wrap" FontSize="14" 
                               LineHeight="20" Foreground="#3C4043"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>