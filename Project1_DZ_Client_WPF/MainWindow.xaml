<Window x:Class="Project1_DZ_Client_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project1_DZ_Client_WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Margin" Value="5"></Setter>
            <Setter Property="Padding" Value="5"></Setter>
            <Setter Property="FontWeight" Value="Bold"></Setter>
            <Setter Property="FontSize" Value="20"></Setter>
        </Style>
        <Style TargetType="{x:Type Label}">
            <Setter Property="FontWeight" Value="Bold"></Setter>
            <Setter Property="FontSize" Value="20"></Setter>
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Margin" Value="5"></Setter>
            <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
        </Style>
    </Window.Resources>
    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Label>IP Address</Label>
            <TextBox Grid.Column="1" x:Name="ipAddressTb"></TextBox>
            <Label Grid.Column="2">Port</Label>
            <TextBox Grid.Column="3" x:Name="portTb"></TextBox>
            <Label Grid.Row="1">Message</Label>
            <TextBox Grid.Column="1" Grid.Row ="1" Grid.ColumnSpan = "2" x:Name="messageTb"></TextBox>
            <Button Grid.Column="3" Grid.Row="1" Click="Button_Click">Send</Button>
        </Grid>
        <Label DockPanel.Dock="Top" HorizontalAlignment="Center">Dialog</Label>
        <ListBox ItemsSource="{Binding messages}"></ListBox>
    </DockPanel>
</Window>